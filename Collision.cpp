#include "Collision.h"

namespace vk {
	std::vector<lineList> hashGrid::createVertices(glm::vec3 dimensions, glm::vec3 resolution)
	{
		std::vector<lineList> vertices;
		return vertices;
	}
	std::vector<uint16_t> hashGrid::createIndices(glm::vec3 dimensions)
	{
		std::vector<uint16_t> indices;
		return indices;
	}
}

